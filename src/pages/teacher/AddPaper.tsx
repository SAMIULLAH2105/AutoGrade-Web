import { Layout } from "@/components/layout/Layout";

export default function TeacherAddPaperPage() {
  return (
    <Layout>
      <div className="container-custom section-padding max-w-3xl mx-auto">
        <h1 className="font-display text-3xl font-bold mb-2">Add New Paper</h1>
        <p className="text-muted-foreground mb-6">Currently disabled.</p>

        <div className="glass-card p-6">
          <p className="text-muted-foreground">
            This screen is reserved for the future “Add New Paper” workflow.
          </p>
        </div>
      </div>
    </Layout>
  );
}
